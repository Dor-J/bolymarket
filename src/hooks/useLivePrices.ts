/**
 * Subscribes to live price updates for the given market IDs.
 * Implementation deferred to Phase 4.
 */
export function useLivePrices(marketIds: string[]): void {
  void marketIds;
  // Phase 4: wire WebSocket or simulation here.
}
