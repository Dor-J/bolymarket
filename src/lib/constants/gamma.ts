/** Category tag slugs fetched in addition to trending events. */
export const GAMMA_CATEGORY_TAG_SLUGS = ["crypto", "sports", "politics"] as const;

/** Max events per Gamma request. */
export const GAMMA_EVENTS_PAGE_LIMIT = 50;

/** Default order field for high-volume events. */
export const GAMMA_EVENTS_ORDER = "volume";
